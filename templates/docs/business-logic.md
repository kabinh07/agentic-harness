# Business Logic

> Generated by `/agentic-harness:configure` from the BRD in `BRD/`. This is the ground truth
> `/agentic-harness:run-manager` evaluates the project against — keep it in priority order.
> Do not hand-edit around it silently; if the BRD changes, re-run `/agentic-harness:configure`
> or edit this file and `config/project.config.yaml`'s `manager.business_goals`
> together so they stay in sync.

*(Not yet configured — run `/agentic-harness:configure` after dropping a BRD into `BRD/`.)*

## Goals (priority order)

1. —

## Success criteria

- —

## Constraints / invariants

- —
